{
  "name": "lsdt-app",
  "version": "1.0.0",
  "description": "LSDT Management App",
  "main": "index.html",
  "scripts": {
    "build": "mkdir dist && cp -r www/* dist/ || xcopy www dist /E /I /H /Y"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/cli": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  }
}
